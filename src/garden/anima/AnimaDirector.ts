import type { GraphData, Tickable } from "../../shared/interfaces.ts";
import { App } from "obsidian";

export class AnimaDirector implements Tickable {


    constructor() {

    }

    public tick(dt: number, nowMs: number): void {
    
    }
}
